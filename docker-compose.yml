services:
  tt-app:
    image: tt
    restart: always
    ports: 
      - '8000:8000'
#    healthcheck:
#      test: ["CMD-SHELL", "curl --silent --fail localhost:8000/flask-health-check || exit 1"]
#      interval: 10s
#      timeout: 10s
#      retries: 3
#    command: guvicorn -w 3 -t 60 -b 0.0.0.0:8000 main:app
    volumes:
      - ./data:/code/data
volumes:
  data: