services:
  tt-app:
    image: transit_tracker:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports: 
      - '8000:8000'
    volumes:
      - ./data:/code/data
volumes:
  data: